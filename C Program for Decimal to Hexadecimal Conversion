#include <stdio.h>

int main() {
    int decimal;
    char hex[20];
    
    printf("Enter a decimal number: ");
    scanf("%d", &decimal);

    sprintf(hex, "%X", decimal); 

    printf("Hexadecimal equivalent of %d is: %s\n", decimal, hex);

    return 0;
}
