#include <stdio.h>

int main() {
    double num;
    char str[50]; 

    printf("Enter a double number: ");
    scanf("%lf", &num);

    sprintf(str, "%lf", num);  

    printf("Double as string: %s\n", str);

    return 0;
}
